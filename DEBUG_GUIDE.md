调试模式使用说明

调试面板

页面右上角有一个调试面板，包含多个开关用于检查地图显示是否正确。打开方式是在 URL 中添加 ?debug=true 参数，或在控制台中设置 localStorage.debugMode = true。

面板开关

台风轨迹开关控制是否显示台风路径线。关闭后可以单独查看其他图层。

事件标记开关控制是否显示事件点。每个事件点旁边会显示标题和类型。

影响范围开关控制是否显示事件周围的圆形区域。半径由事件数据中的 impactRadius 字段决定。

投影开关将事件点移动到对应时间的轨迹位置附近，用于检查事件和轨迹的时间对应关系。

图层叠加开关分别控制夜光图层和降雨图层的显示。

坐标参考

调试模式下会显示台北 101 大楼的位置作为参考点。台北 101 坐标：纬度 25.0340，经度 121.5645。如果这个点位置正确，说明地图坐标系没有问题。

检查项目

台风路径是否从东南向西北推进（符合西北太平洋台风常见路径）。
事件点是否在合理时间内出现（生成、警报、登陆、转向等）。
坐标顺序是否正确（先纬度后经度传给 Leaflet）。
夜光和降雨图层是否正确叠加在底图上。

常见问题排查

参考点位置不对：检查坐标是否写反了（应该是 [lat, lng] 而不是 [lng, lat]）。

事件点离轨迹太远：确认是否开启了投影开关。

图层不显示：检查 localStorage 中的数据源设置，确认静态资源路径正确。

影响范围太大或太小：修改事件数据中的 impactRadius 字段值。
