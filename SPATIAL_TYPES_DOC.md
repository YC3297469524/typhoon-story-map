空间数据类型与坐标系统说明

坐标系统

Leaflet 使用 [纬度, 经度] 顺序存储和传递坐标。数据文件中的对象通常用 lat 和 lng 两个字段分别保存，传给地图组件时组装成数组。这种分离方式可以避免顺序混淆。

项目中的坐标处理遵循以下规则：数据文件用 lat/lng 字段，组件内用数组格式 [lat, lng]，画圆时半径单位是米。

空间分类

精确空间指有明确地理坐标的点，包括台风路径点、台北 101 参考点。这类点的坐标来自实际测量或官方数据。

半抽象空间指根据时间关系投影到轨迹附近的点，主要是事件标记。事件的具体位置可能无法精确获知，但可以投影到对应时间的台风轨迹附近。

影响空间指用圆形表达影响范围的点。圆心是事件位置，半径由 impactRadius 字段决定。单位是米。

区域空间指用多边形或热力图表达的区域，如夜光强度分布、降雨量分布。这类数据没有单个精确坐标点，而是覆盖一片区域。

调试模式显示

调试模式会同时展示多种空间类型。台风轨迹用折线连接路径点。事件标记用彩色图标区分类型。影响范围用半透明圆形表示。夜光和降雨用格网或热力方式叠加。

验证方法

验证坐标正确性主要看三个层面。轨迹点是否从东南向西北移动（正常台风路径）。事件是否在合理时间内出现。区域数据是否覆盖了预期范围。

台北 101 位置验证

台北 101 坐标（25.0340, 121.5645）在调试模式下应显示在台湾岛北部。如果位置偏差很大，说明坐标处理有问题。
